meta {
  name: Get current user
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/api/user
  body: none
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
